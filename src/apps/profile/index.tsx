"use client";

import { ProfileApp as ProfileAppInner } from "@/components/ui/profile-app";
import type { AppProps } from "@/types";

export function ProfileApp(_: AppProps) {
  return <ProfileAppInner />;
}
